# Feature 01 — CLI Shell Checklist

## 当前有效验收 — 极简 CLI Shell

- [x] 启动显示分隔线、EvoCode、实际 Version、Model: Not configured，随后显示纯文本输入提示。
- [x] 8 项自动化测试通过，覆盖启动参数、版本、连续输入、帮助、未知命令、空白输入、EOF 和中断。
- [x] 已安装入口从其他工作目录可启动并正常退出。
- [x] 2026-09-11 实际运行 `uv run evocode`，依次提交 hello、/help、/exit，提示正确并以状态码 0 退出。
- [x] 保留模型未接入提示，不执行模型或工具。

图标、字体、配色、面板、Path、Mode 等历史视觉验收项由用户取消，不再待验收。以下仅为历史记录。


状态：自动化及 PTY 端到端检查通过；图形终端人工视觉验收待完成。

## 工程与入口

- [x] 执行 `uv sync` 成功，生成并保留 uv.lock；新增运行时依赖只有 Rich，无新增测试框架依赖。
- [x] 执行 `uv run evocode --help`，显示英文使用帮助并以状态码 0 退出，不进入输入循环。
- [x] 执行 `uv run evocode --version`，显示与已安装包元数据一致的版本并以状态码 0 退出。
- [x] 执行 `uv run python -m unittest discover -s tests -v`，实际发现并运行 CLI/UI 测试，全部通过。

## 显示与视觉

- [x] 执行 `uv run evocode`，欢迎区显示 EvoCode、实际包版本、`Model: Not configured` 和启动时的实际工作目录。
- [x] 在另一个工作目录调用已安装 CLI，Path 对应该目录，而非固定项目路径。
- [ ] 深色终端中人工对照参考图：蓝灰主色、暖橙强调、字符品牌/机器人、信息分区、输入前留白；无全屏 Dashboard。
- [ ] 在 100、60、40 列终端检查布局；窄屏简化装饰，Version / Model / Path 仍可读，无边框错位或文字相互覆盖。
- [x] 路径长度超过终端宽度时换行显示，不因长路径崩溃。
- [ ] 使用 `NO_COLOR=1 uv run evocode`，显示仍可读且不输出颜色控制序列。
- [x] 路径或输入包含 `[red]` 等标记时，不被解释为终端样式，不触发解析异常。

## 交互

- [x] 输入普通文本并回车，显示 `Model is not connected yet.`，随后再次显示输入提示，不调用模型或执行工具。
- [x] 提交空行或仅空白字符，不显示未连接提示或错误，继续等待输入。
- [x] 输入 `/help`，显示 `/help`、`/exit` 的用途及 Ctrl+C / Ctrl+D 退出说明，随后继续交互。
- [x] 输入 `/unknown`，显示英文未知命令提示及 `/help` 指引，随后继续交互。
- [x] 输入 `/exit`，以状态码 0 退出，不出现 traceback。
- [x] 在等待输入时按 Ctrl+C，以状态码 0 退出，不出现 traceback。
- [x] 在空输入提示处按 Ctrl+D，以状态码 0 退出，不出现 traceback。
- [x] 向标准输入提供有限的多行文本，处理完毕后在 EOF 正常退出，不出现无限循环。

## End-to-End

- [x] **真实终端 E2E**：执行 `uv sync` → `uv run evocode` → 核对品牌、版本、模型状态和目录 → 连续提交三次普通文本 → `/help` → `/unknown` → `/exit`；每步符合上述标准，过程保留终端滚动记录，最终状态码为 0。

## 验收记录

实现：已编写。自动化测试：8 项通过。主流程：已集成。端到端：PTY 交互通过；人工视觉验收待完成。

2026-09-11，macOS，uv 使用 CPython 3.13.13：

- `uv sync` 成功；`uv run python -m unittest discover -s tests -v` 实际运行 8 项测试并全部通过。
- `uv run evocode` 在 PTY 中启动，核对欢迎区及目录；连续三条普通文本、帮助、未知命令、退出均符合预期，状态码 0。
- 单独启动已安装 CLI，通过 PTY 发送 Ctrl+C、Ctrl+D，均以状态码 0 退出，无 traceback。
- 自动化验证 100/60/40 列输出宽度、长路径、字面标记、外部目录启动、无颜色子进程输出和 EOF。
- 尚未在图形终端人工核对真实字体、颜色和三个窗口宽度；不将文本输出检查等同于视觉验收。
- MewCode 来源仍缺少可见路径，本轮未参考其代码。
- README.md 与 AGENTS.md 的用户改动保留；未执行 Git commit 或 push。

执行时记录日期、系统、终端宽度、命令、结果与限制；只有实际通过的项目才勾选。

## 品牌视觉迭代 — 2026-09-11

- 按用户图四重绘共享像素机器人：圆头、双眼、短身体、代码胸章、天线及轨道星标。终端使用半块字符，上下两个像素合并为一行；宽度至少 100 列时展示，窄屏保留紧凑信息区。
- README 顶部新增本地 SVG 标识、品牌横幅、方向标签和 CLI 渲染示意；原正文保留。预览路径使用示例路径，不作为运行截图。
- 8 项测试通过；修复路径折行断言，并指定测试终端高度，确保 100/60/40 列不被 dumb-terminal 默认宽度覆盖。
- 图形终端实际字体与 GitHub 页面视觉仍待用户查看；未提交或推送。

## CLI Visual Refinement — 最新视觉标准

本节覆盖此前品牌布局描述；交互验收标准不变。

- [x] Mascot 使用独立 renderer，28 列 × 11 行；宽头、深色脸、双眼、短身体、暖橙天线与字面 `</>` 胸章，无 Emoji。
- [x] 宽屏采用左右布局；右侧 5 行双色 EvoCode 像素字，版本在右侧，副标题为 `Evaluable Coding Agent`。
- [x] 信息区显示 Model / Path / Mode；Mode 为 `Interactive (local)`；用户主目录以 `~` 缩写，版本不再重复列出。
- [x] 圆角边框、两条分隔线、底部提示与欢迎输入区域已按本轮参考调整。
- [x] 8 项自动化回归测试通过；100/60/40 列输出不溢出，原交互行为保持不变。
- [ ] 用户在实际深色终端中对照最新参考图确认字号比例、字形和配色。

110 列 SVG 预览已更新到 `assets/branding/terminal-preview.svg`，为实际 renderer 的示例输出；尚未宣称视觉验收完成。未修改 README 正文或 AGENTS，未提交或推送。

## Brand Lock

唯一基准已原样保存为 assets/evocode-brand-source.png；约束见 assets/BRAND.md 与 AGENTS.md 新增节。固定字符稿与固定字标行不再允许自行重新设计。

本轮 8 项测试通过；110 列 PTY 实际执行 uv run evocode，信息与输入正常，/exit 返回 0。SVG 为相同 renderer 的彩色输出预览，不冒充截图。独立 PNG 名称已预留，尚无独立原始导出；视觉匹配仍待用户确认。

## 最新覆盖：无图标单栏头部

用户已撤销 Brand 方案，以最新单栏截图为基准。本次删除残余 branding.py，移除机器人、外框与欢迎语；三行扁平像素字、右侧版本、单栏字段、两条分隔线、同一行帮助/退出提示。原功能保持。此前机器人与品牌布局验收项不再适用。

实际执行 uv run evocode，/exit 返回 0；测试通过。字符排布已调整，终端字体与行高由用户终端控制，尚不宣称像素级相同或视觉验收完成。
